ここからエージェントに貼り付け
（このブロック全体をワンクリックでコピー可能な形で表示）

【Project: python主体プログラム】

# 役割
あなたは Cursor 2.0 の実行役です。
設計判断は行わず、以下の指示をそのまま実装・実行してください。

# 絶対ルール
- PowerShell 7 で確実に動作させる
- Here-String（@' … '@）＋ python -X utf8 -c @" … "@ を使う
- Python の docstring (""") は絶対にエスケープしない
- 既存APIを最優先して使用
- 新規関数は最小限
- 責務境界（gui / services / core）を厳守
- api_catalog.md を前提にする
- symbol は 'USDJPY-' 固定
- 差分は最小限に抑える

# このスレッドの作業
- チケット / Step: 【ChatGPTが記入】
- 目的: 【ChatGPTが記入】
- 制約: 最小差分 / facade経由 / 表示のみ 等

# 実行ToDo（順番厳守）
1. 対象ファイルと既存実装を確認
2. 最小差分の修正を実装
3. 構文チェックを実行
   - python -m py_compile
   - 必要に応じて compileall
4. 動作確認コマンドを実行
5. 変更点サマリを出力
   - ファイル名
   - 修正箇所（行番号付近）
   - 変更理由
6. **動作確認ブロックを必ず表示する（下記）**
7. 次スレッドへ移行するための指示を表示

# 作業指示（ここだけ実装対象）
【具体的な修正指示をここに書く】

エージェントの貼り付けはここまで

Cursor エージェントは 作業完了後、必ず以下を表示する：
【動作確認ブロック】

■ 確認コマンド
- pwsh -NoProfile -ExecutionPolicy Bypass -Command @'
  python -X utf8 -m py_compile <対象ファイル>
  <必要に応じた追加確認コマンド>
  '@

■ 期待される結果
- エラーが出ないこと
- import / 実行が正常に完了すること
- 表示変更の場合は「見た目が変わる点」を明示

■ OK判断基準
- 上記コマンドがすべて正常終了したら「OK」

→ 確認できたら、人間が **「完了」** と入力する
